// file for the vectors in rust 

fn main()
{
	let a = [1,2,3];  //  a simple array in the rust

	let mut b = vec![1,2,3,4]; // to make the array for the rust

	b.push(5);  // appending to the vector

	println!("{}",a[0]);

	println!("{}",b[4])
}